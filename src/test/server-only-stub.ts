// Test stub for the `server-only` package. In production Next aliases
// `server-only` to a throwing/empty module depending on the bundle; in unit
// tests we alias it here to a no-op so server modules can be imported.
export {};
